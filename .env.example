# Frontend (Next.js) - API base URL for Laravel backend
# Copy to .env.local and set your backend URL
# Default when backend runs with: cd backend/quickcon-api && php artisan serve
NEXT_PUBLIC_API_URL=http://localhost:8000/api
